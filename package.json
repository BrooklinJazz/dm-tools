{
  "name": "dm-tools",
  "version": "1.0.0",
  "description": "a tool for running dungeons and dragons encounters",
  "author": "Brooklin Myers",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.0.0",
    "cross-env": "^5.2.0",
    "express": "^4.16.3",
    "nuxt": "^2.0.0",
    "socket.io": "^2.1.1",
    "socket.io-client": "^2.1.1",
    "vuetify": "^1.2.4"
  },
  "devDependencies": {
    "@mdi/font": "^3.0.39",
    "nodemon": "^1.11.0",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
